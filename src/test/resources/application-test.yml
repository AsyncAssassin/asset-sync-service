spring:
  jooq:
    sql-dialect: postgres
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml

asset-sync:
  outbox:
    scheduler:
      enabled: false
    retention:
      enabled: false
  sync:
    recovery:
      enabled: false
    worker:
      enabled: false
